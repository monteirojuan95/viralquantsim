% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_sequencing.R
\name{simulate_sequencing}
\alias{simulate_sequencing}
\title{Simulate sequencing-derived target reads}
\usage{
simulate_sequencing(data, parameters = default_parameters())
}
\arguments{
\item{data}{A data frame containing \code{target_log10_available}.}

\item{parameters}{A complete parameter list.}
}
\value{
The input data frame with sequencing columns.
}
\description{
Generates total reads, a sample-specific target fraction, target read
counts, and stochastic dropout. A beta-binomial construction is used to
represent overdispersion while ensuring target reads cannot exceed total
reads.
}
\examples{
input <- data.frame(target_log10_available = c(3, 4, 5))
seq <- simulate_sequencing(input)
seq[, c("total_reads", "target_reads", "log10_target_reads")]
}
